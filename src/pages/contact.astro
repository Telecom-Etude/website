---
import { t, changeLanguage } from "i18next";
import Layout from "../layouts/Layout.astro";
import Contact from "../website-data/content/Contact.svelte";

changeLanguage("fr");
const translations = t("contact", { returnObjects: true });
---

<Layout title="contact.title">
    <Contact client:load t={translations} />
</Layout>
